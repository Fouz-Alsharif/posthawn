/**
 * Production: set window.HAWN_API_BASE to your deployed API URL (HTTPS, no trailing slash).
 * Example: window.HAWN_API_BASE = 'https://hawn-api.onrender.com';
 * Local dev: keep http://127.0.0.1:8000 and run Backend/main.py on your machine.
 */
window.HAWN_API_BASE = "https://hawn-api.onrender.com";
